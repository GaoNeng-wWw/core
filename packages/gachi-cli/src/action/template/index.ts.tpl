import {spider,urls,engine,Box,customConfig} from 'gachi';
export class {{name}} extends spider{
    constructor(){super()}
    public urls: urls = [];
    public open(config: customConfig): void {
        
    }
    public async run(this: spider,engine: engine, res: Box, config: customConfig) {

    }
    public shutdown(config: customConfig): void {
        
    }
}
